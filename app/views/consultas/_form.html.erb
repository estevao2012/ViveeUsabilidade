<div class="col-sm-12">
  <div class="panel panel-default"> 
    <div class="panel-heading">
      <h3 class="panel-title">Marcar consulta </h3>
    </div>
    <div class="panel-body">   

    <%= form_for @consulta  do |f| %>
      <% if @consulta.errors.any? %>
      <div id="error_explanation">
        <h2><%= pluralize(@consulta.errors.count, "error") %> prohibited this consulta from being saved:</h2>

        <ul>
          <% @consulta.errors.full_messages.each do |message| %>
          <li><%= message %></li>
          <% end %>
        </ul>
      </div>
      <% end %>


      <div class="form-group">
        <%= f.label :data %> <br>
        <%= f.date_select :data,{} ,{ class: "form-control data-part"}  %>
      </div>
      <div class="form-group">
        <%= f.label :horario %> <br>
        <%= f.time_select :data, { :ignore_date => true } , { class: "form-control time-part"} %>
      </div>

      <div class="form-group">
        <%= f.label :medico_id, "Médico" %>
        <%= f.select :medico_id, Medico.all.collect {|p| [ p, p.id ] } , {} , class: "form-control"  %>
      </div>
  
        <%= f.hidden_field :usuario_id, class: "form-control"  %> 
      <div class="actions">
        <%= f.submit "Enviar", class: "btn btn-success"%>
      </div>
      <% end %> 
    </div>
  </div> 
</div>

